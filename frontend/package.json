{
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix sea-battle",
    "build": "npm run build --prefix sea-battle",
    "preview": "npm run preview --prefix sea-battle",
    "lint": "npm run lint --prefix sea-battle"
  },
  "dependencies": {
    "axios": "^1.13.6",
    "socket.io-client": "^4.8.3",
    "zustand": "^5.0.11"
  }
}
